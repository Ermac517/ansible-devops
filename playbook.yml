- hosts: all
  become: true
  tasks:
    - name: Ensure chrony is installed
      dnf:
        name: chrony
        state: present

    - name: Ensure chronyd service is started and enabled
      service:
        name: chronyd
        state: started
        enabled: true
